import { DateControllerDirective } from './date-controller.directive';

describe('DateControllerDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new DateControllerDirective();
    expect(directive).toBeTruthy();
  });
});
