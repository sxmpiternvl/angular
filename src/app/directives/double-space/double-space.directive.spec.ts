import { DoubleSpaceDirective } from './double-space.directive';

describe('DoubleSpaceDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new DoubleSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
