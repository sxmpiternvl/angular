import { RegErrorDirective } from './reg-error.directive';

describe('RegErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new RegErrorDirective();
    expect(directive).toBeTruthy();
  });
});
